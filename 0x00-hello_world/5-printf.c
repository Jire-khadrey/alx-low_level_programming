#include <stdio.h>
/**
 *main - prints exactly the sentence,
 *with proper grammar,
 *but the outcome is a piece of art,
 *Return: 0 if processed properly.
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
