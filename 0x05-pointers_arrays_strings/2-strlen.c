#include "main.h"
#include <string.h>

/**
 * _strlen - A function that returns the length of a string
 * @s: String input value
 * Return: 0
 */

int _strlen(char *s)
{
	int len;

	len = strlen(*s);

	return (len);
}
